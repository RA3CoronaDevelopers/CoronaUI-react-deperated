import React from 'react';
import './inputer.scss';

export interface ITextInputerProps {}

export const TextInputer: React.FC<ITextInputerProps> = ({}) => {
  return <div />;
};

export interface IFileInputerProps {}

export const FileInputer: React.FC<IFileInputerProps> = ({}) => {
  return <div />;
};
